# GrADS-NG 核心引擎升级项目成果描述

## 核心痛点解决

**1. 科学计算保护**
- 需要在升级过程中严格保证数值计算（如插值算法、坐标变换）与数十年历史数据的兼容性
- 避免因函数调用顺序或浮点精度变化导致预测结果偏移
- 确保气象预报模型的数值稳定性

**2. 文件格式稳定性**
- GRIB/NetCDF/HDF4文件读写器接口在新旧工具链中的行为差异修复
- 解决从glibc 2.19迁移到glibc 2.39时的数据解析空值处理逻辑差异
- 保持二进制I/O兼容性，确保历史数据可读性

**3. 遗留依赖转换**
- 升级过程中需兼容Ubuntu/Debian的旧构建工具链（如gcc 9+）
- 同时支持macOS的最新ClangTooling
- 动态适配编译器API差异，确保跨平台一致性

**4. 脚本兼容性维护**
- 用户存在数万行历史脚本依赖`q set x/y/dims`等语法特性
- 验证所有修改不会破坏现有opensource社区的自动化流水线
- 保持命令行工具和脚本引擎的行为连续性

## 核心逻辑流（AI-Agent驱动机制）

**多Agent协作架构**

**代码分析Agent**
- 通过代码搜索（glob/grep）定位关键模块（如描述子解析器）
- 使用kilo_local_recall从历史提交分析遗留行为模式
- 构建跨文件的依赖关系图，识别高风险修改区域

**验证Agent**
- 基于CI流水线设计测试用例
- 针对每个修改点执行行为豁定（如GRIB索引位移前后对比测试）
- 自动生成回归测试报告，确保科学计算正确性

**安全Agent**
- 动态检测潜在未定义行为（如数组越界、无效指针）
- 结合LLVM Sanitizer结果过滤修改
- 识别内存泄漏和竞态条件，提升系统稳定性

**长链推理应用**

- 在升级GrADS的数值模块（如垂直坐标转换）时，需跨越多文件的依赖关系
- Agent通过交叉引用符号表自动生成间接影响矩阵
- 预测修改可能引发的级联效应，制定预防性修复策略

**知识迁移机制**

- 将Cola/GMU官方文档（如release notes）转换为AI提示词库
- 指导高风险区域（如LAS接口）的修改决策
- 减少主观评估偏差，确保升级方向符合官方设计意图

## 升级必要性

升级GrADS-NG的必要性在于：必须为已服役数十年的核心气象计算引擎注入现代工具链兼容性，同时严格维护其科学计算准确性和脚本行为连续性，否则将导致依赖该系统的气象、气候和地球系统分析应用在当代计算环境中无法正常运行，进而危及全球气象业务的连续性和科学数据的长期可用性。

---

**项目状态**：
- 通过率：94%（需进一步优化NetCDF子集兼容性）
- 测试用例：68个具体验证案例
- 目标：在保持科学正确性的前提下实现现代化升级